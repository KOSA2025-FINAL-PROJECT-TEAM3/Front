# AMApill Frontend - Production Environment Variables
# Use this template for production deployments

# ==============================================================================
# PRODUCTION CONFIGURATION (Nginx deployment)
# ==============================================================================

# API Configuration (Nginx proxy on 80/443)
VITE_USE_NGINX=true
# Leave empty to use window.location for auto-detection via Nginx proxy
VITE_API_BASE_URL=

# WebSocket Configuration
# Leave empty to use window.location for auto-detection
VITE_WS_BASE_URL=
VITE_WS_PORT=8080

# Frontend URL (your production domain)
VITE_FRONTEND_URL=https://amapill.example.com

# Kakao OAuth (Production credentials)
VITE_KAKAO_CLIENT_ID=your_production_kakao_client_id_here
VITE_KAKAO_REDIRECT_URI=https://amapill.example.com/auth/kakao/callback

# Production Flags
VITE_ENV=production
VITE_DEBUG=false
VITE_ENABLE_DEV_MODE=true
VITE_USE_MOCK_API=true

# API Timeout (milliseconds)
VITE_API_TIMEOUT=10000

# ==============================================================================
# SHARED CONFIGURATION
# ==============================================================================

# Storage Keys
VITE_TOKEN_STORAGE_KEY=amapill_token
VITE_REFRESH_TOKEN_KEY=amapill_refresh_token

# UI Settings
VITE_NOTIFICATION_TIMEOUT=5000
VITE_ITEMS_PER_PAGE=10
VITE_MAX_FILE_SIZE=5242880  # 5MB in bytes

# ==============================================================================
# DEPLOYMENT INSTRUCTIONS
# ==============================================================================
# 1. Copy this file to .env.production
# 2. Update VITE_FRONTEND_URL to your domain
# 3. Add your production Kakao client ID
# 4. Build with: npm run build -- --mode production
# 5. Deploy dist/ to Nginx
