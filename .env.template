# AMA...Pill Frontend Environment Variables
# 이 파일을 복사하여 .env 파일을 만들고 실제 값을 입력하세요.
# Copy this file to .env and fill in the actual values.

# ==============================================================================
# DEVELOPMENT CONFIGURATION (Vite dev server - localhost:5173)
# ==============================================================================
# For local development, these point to direct microservice ports

# API 서버 설정 (Microservices - Development)
VITE_USE_NGINX=false
VITE_AUTH_API_URL=http://localhost:8081        # Auth Service
VITE_MEDICATION_API_URL=http://localhost:8082  # Medication Service
VITE_FAMILY_API_URL=http://localhost:8082      # Family Service
VITE_DIET_API_URL=http://localhost:8082        # Diet Service
VITE_SEARCH_API_URL=http://localhost:8082      # Search Service
VITE_CHAT_API_URL=http://localhost:8082        # Chat Service

# API Gateway (Development)
VITE_API_BASE_URL=http://localhost:8082
VITE_API_TIMEOUT=10000

# WebSocket 서버 설정 (Development)
VITE_WS_BASE_URL=ws://localhost:8080

# 인증 관련 (Development)
VITE_KAKAO_CLIENT_ID=your_kakao_client_id_here
VITE_KAKAO_REDIRECT_URI=http://localhost:5173/auth/kakao/callback
VITE_FRONTEND_URL=http://localhost:5173

# 개발 환경 플래그
VITE_ENV=development
VITE_DEBUG=true

# 개발자 도구 패널 활성화 (true로 설정 시 화면에 "🧪 Dev Mode" 버튼 표시)
# Enable Developer Mode Panel (shows "🧪 Dev Mode" button when set to 'true')
VITE_ENABLE_DEV_MODE=false

VITE_USE_MOCK_API=false

# ==============================================================================
# PRODUCTION CONFIGURATION (Nginx deployment - port 80/443)
# ==============================================================================
# For production, comment out development vars and use these:
#
# # Production Mode (Nginx Proxy)
# VITE_USE_NGINX=true
# # Leave URLs empty to auto-detect via Nginx reverse proxy on port 80/443
# VITE_API_BASE_URL=
#
# # WebSocket Configuration (Production)
# VITE_WS_BASE_URL=
# VITE_WS_PORT=8080
#
# # Frontend URL (Production)
# VITE_FRONTEND_URL=https://amapill.example.com
#
# # Kakao OAuth (Production)
# VITE_KAKAO_CLIENT_ID=your_production_kakao_client_id
# VITE_KAKAO_REDIRECT_URI=https://amapill.example.com/auth/kakao/callback
#
# # Production Flags
# VITE_ENV=production
# VITE_DEBUG=false
# VITE_ENABLE_DEV_MODE=false
# VITE_USE_MOCK_API=false

# ==============================================================================
# SHARED CONFIGURATION (Both dev and production)
# ==============================================================================

# 토큰 저장 위치
VITE_TOKEN_STORAGE_KEY=amapill_token
VITE_REFRESH_TOKEN_KEY=amapill_refresh_token

# 알림 설정
VITE_NOTIFICATION_TIMEOUT=5000

# 페이지 설정
VITE_ITEMS_PER_PAGE=10
VITE_MAX_FILE_SIZE=5242880  # 5MB in bytes
